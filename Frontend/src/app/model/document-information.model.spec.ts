import { DocumentInformation } from './document-information.model';

describe('DocumentInformation', () => {
  it('should create an instance', () => {
    expect(new DocumentInformation()).toBeTruthy();
  });
});
