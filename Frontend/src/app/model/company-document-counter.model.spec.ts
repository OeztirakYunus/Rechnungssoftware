import { CompanyDocumentCounter } from './company-document-counter.model';

describe('CompanyDocumentCounter', () => {
  it('should create an instance', () => {
    expect(new CompanyDocumentCounter()).toBeTruthy();
  });
});
