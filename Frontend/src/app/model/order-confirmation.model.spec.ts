import { OrderConfirmation } from './order-confirmation.model';

describe('OrderConfirmation', () => {
  it('should create an instance', () => {
    expect(new OrderConfirmation()).toBeTruthy();
  });
});
